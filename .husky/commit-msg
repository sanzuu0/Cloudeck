#!/usr/bin/env sh
# commit-msg: enforce Conventional Commits via commitlint

# If commitlint is available, validate the message file ($1)
if command -v npx >/dev/null 2>&1; then
  exec npx --yes @commitlint/cli --edit "$1"
else
  echo "ℹ️ npx not found → skipping commit message lint."
  exit 0
fi
